<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.sdhacks2019">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".Breakfast1Activity"
            android:label="@string/title_activity_breakfast1"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".DinnerActivity" />
        <activity android:name=".LunchActivity" />
        <activity android:name=".BreakfastActivity" />
        <activity android:name=".DailyActivity" />
        <activity
            android:name=".MealActivity"
            android:label="@string/title_activity_meal"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".DietaryActivity" />
        <activity android:name=".DietActivity" />
        <activity android:name=".MainActivity" />
        <activity
            android:name=".Main1Activity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>